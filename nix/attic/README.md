# attic/

Files we don't currently use, kept here so `git log --follow` works if we
ever want to revive them. Nothing in this directory is imported by the
flake.

Current contents:

- `zsh.nix`  – zsh + oh-my-zsh + powerlevel10k home-manager config from when
  zsh was the daily-driver shell. Has stale references (Fig, Intel-mac
  homebrew paths, deprecated `exa`); resurrect with care.
- `opencode.nix` – `programs.opencode` enable stub from a brief experiment.
