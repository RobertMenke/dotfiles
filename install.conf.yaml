- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.config: config
    ~/.oh-my-zsh/custom: zsh_custom
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
