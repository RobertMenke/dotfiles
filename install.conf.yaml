- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.config/lvim: lvim
    ~/.config/iterm2: iterm2
    ~/.config/alacritty.yml: alacritty.yml
    ~/.oh-my-zsh/custom: zsh_custom
    ~/.zshrc: zshrc
